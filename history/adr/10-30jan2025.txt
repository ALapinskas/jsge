Date: January 30 2025
jsge@1.5.4
Merge draw calls / Доработать метод _bindRenderObject()

Status: In Progress

Context:
    _bindRenderObject() включается в себя части подготовки объекта
    с помощью включенной с помощью extension логики подготовки, отправки на рендер
    и отдельно те же операции с захардкоженным bind() для изображений. Это сделано для того
    чтобы можно было отрисовывать объекты которые были унаследованы от DrawImageObject, а также рисовать границы.
     

Decision:
    Нужно изменить это поведение следующим образом:
    1. Программа для отрисовки должна определятся и по имени класса, и по типу объекта, полю renderObject.type

Impact:
    No impact.